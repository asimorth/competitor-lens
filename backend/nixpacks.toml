# Nixpacks Configuration for Railway
# NO PRISMA COMMANDS - Database already has schema!

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps --ignore-scripts"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node start-railway.js"
